// Firebase is not used in this Replit deployment.
// Authentication is handled via Replit Auth (X-Replit-User-* headers).
// This stub prevents import errors from files that still reference this module.

export function isFirebaseConfigured(): boolean {
  return false;
}

export function getFirebaseAuth(): null {
  return null;
}
